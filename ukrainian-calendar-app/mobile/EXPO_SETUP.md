# 📱 Налаштування для тестування на iPhone через Expo Go

## ✅ Що вже зроблено:
- ✅ Expo додано до проєкту
- ✅ Конфігурація app.json налаштована
- ✅ Скрипти для запуску додано

## 🚀 Як запустити на iPhone:

### Крок 1: Встановіть Expo Go на iPhone
1. Відкрийте App Store на iPhone
2. Знайдіть та встановіть **Expo Go** (безкоштовний додаток)

### Крок 2: Запустіть Expo сервер
```bash
cd ukrainian-calendar-app/mobile
npm run expo
```

Або з тунелем (якщо iPhone в іншій мережі):
```bash
npm run expo -- --tunnel
```

### Крок 3: Підключіть iPhone

**Варіант А: QR код (найпростіше)**
1. Після запуску `npm run expo` з'явиться QR код у терміналі
2. Відкрийте **Expo Go** на iPhone
3. Натисніть "Scan QR code"
4. Відскануйте QR код з терміналу

**Варіант Б: Через URL**
1. У терміналі з'явиться URL типу `exp://192.168.x.x:8081`
2. Відкрийте **Expo Go** на iPhone
3. Введіть URL вручну або відскануйте QR код

**Варіант В: Через тунель (для різних мереж)**
```bash
npm run expo -- --tunnel
```
Це створить публічний URL, який працює з будь-якої мережі.

### Крок 4: Очікуйте завантаження
- Expo Go завантажить JavaScript bundle
- Додаток запуститься на iPhone
- Ви побачите "📅 Український Календар"

## 🔧 Налаштування для роботи з backend API

Якщо потрібно підключитися до backend API:

1. Знайдіть IP адресу вашого комп'ютера:
```bash
hostname -I | awk '{print $1}'
```

2. Оновіть конфігурацію API (коли створите файл `src/config/api.ts`):
```typescript
export const API_BASE_URL = 'http://YOUR_IP:3000/api';
```

3. Переконайтесь, що iPhone і комп'ютер в одній Wi-Fi мережі

## 📝 Примітки

- **Expo Go** має обмеження - не всі нативні модулі працюють
- Для повної функціональності (Firebase, push-нотифікації) може знадобитися Development Build
- Якщо виникають проблеми, спробуйте `npm run expo -- --clear`

## 🆘 Вирішення проблем

**Помилка "Unable to resolve module"**
```bash
npm run expo -- --clear
```

**Додаток не завантажується**
- Перевірте, що iPhone і комп'ютер в одній мережі
- Спробуйте використати `--tunnel` режим

**QR код не сканується**
- Використайте URL вручну в Expo Go
- Або надішліть URL через повідомлення на iPhone

