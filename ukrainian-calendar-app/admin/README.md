# 🎨 Admin Panel - Ukrainian Calendar

Простий HTML/JavaScript інтерфейс для керування подіями календаря.

## 🚀 Швидкий старт

### Варіант 1: Відкрити напряму

Просто відкрийте `index.html` у браузері:

```bash
open admin/index.html  # macOS
xdg-open admin/index.html  # Linux
start admin/index.html  # Windows
```

### Варіант 2: Через веб-сервер

```bash
# З використанням Python
cd admin
python3 -m http.server 8080

# Або з використанням Node.js (npx)
npx http-server -p 8080

# Відкрийте http://localhost:8080
```

## ✨ Функціонал

### Dashboard (Головна)
- 📊 Статистика в реальному часі:
  - Всього подій
  - Заповнених подій
  - Кількість зображень
  - Активних пристроїв

### Події
- 📅 Перегляд всіх подій
- 🔍 Фільтрація за місяцем
- 📖 Детальна інформація про кожну подію
- ✏️ Швидке редагування (кнопка на картці)

### Редагування
- 🆔 Пошук події за ID
- ✍️ Редагування всіх полів:
  - Назва
  - Опис
  - Традиції
  - Підготовка
- 💾 Збереження змін

### Налаштування
- ⚙️ Конфігурація API endpoint
- 📝 Список доступних API endpoints

## 🔧 Налаштування

### Зміна API URL

1. Перейдіть на вкладку "Налаштування"
2. Введіть URL вашого Backend API
3. Натисніть Enter (автоматично зберігається в localStorage)

**За замовчуванням:** `http://localhost:3000/api`

**Для віддаленого сервера:**
```
http://your-server-ip:3000/api
https://api.yourdomain.com/api
```

## 🎨 Інтерфейс

- **Світла тема** - зручно для очей
- **Responsive design** - працює на всіх пристроях
- **Інтуїтивна навігація** - вкладки та форми
- **Миттєвий фідбек** - повідомлення про успіх/помилки

## 📡 Використовувані API Endpoints

### GET запити:
- `GET /api/events` - Всі події
- `GET /api/events/:id` - Одна подія
- `GET /api/events/month/:month` - Події за місяць
- `GET /api/admin/stats` - Статистика

### PUT запити:
- `PUT /api/admin/events/:id` - Оновлення події

## 🔒 Безпека

⚠️ **Увага:** Це базова адмін-панель без автентифікації!

Для production рекомендується:
- Додати систему авторизації (JWT токени)
- Використовувати HTTPS
- Обмежити доступ до `/api/admin/*` endpoints
- Додати rate limiting

## 🚧 TODO (Майбутні покращення)

- [ ] Авторизація користувачів
- [ ] Завантаження зображень через інтерфейс
- [ ] Масове редагування подій
- [ ] Експорт/імпорт CSV
- [ ] Попередній перегляд подій (як в мобільному додатку)
- [ ] Графіки та аналітика
- [ ] Темна тема

## 💡 Поради

1. **CORS проблеми?**
   - Переконайтесь що backend запущений
   - Перевірте що CORS налаштований в backend (.env файл)

2. **Не відображаються події?**
   - Перевірте API URL в налаштуваннях
   - Відкрийте Dev Tools (F12) → Console для помилок

3. **Не зберігаються зміни?**
   - Перевірте що використовуєте правильний ID події
   - Перевірте що backend доступний

## 📝 Приклад використання

1. Запустіть backend: `cd backend && npm run dev`
2. Відкрийте admin panel в браузері
3. Перейдіть на Dashboard → побачите статистику
4. Перейдіть на Події → оберіть місяць
5. Натисніть "Редагувати" на будь-якій події
6. Внесіть зміни → Збережіть

## 📄 Ліцензія

MIT
